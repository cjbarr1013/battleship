export function DOMManager() {
  const fillSquare = (status) => {
    switch (status) {
      case 'hit':
        // apply hit class
        break;
      case 'miss':
        // apply miss class
        break;
      case 'unharmed':
        // apply unharmed class
        break;
      case 'empty':
        // apply empty class
        break;
    }
  };
}
